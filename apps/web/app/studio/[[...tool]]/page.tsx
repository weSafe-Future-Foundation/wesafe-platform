export default function StudioPage() {
  return (
    <div style={{ padding: "2rem", textAlign: "center" }}>
      <h1>Studio Route Works!</h1>
      <p>If you see this, the route is fine and we just need to fix the Sanity Studio import.</p>
    </div>
  );
}
